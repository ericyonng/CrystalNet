# MPMCQueue 多生产者多消费者队列

入队操作：

从头部获取下一个写入票据。
等待轮次（2 * (票据 / 容量)）以写入槽位（票据 % 容量）。
设置轮次 = 轮次 + 1，通知读取者写入完成。

出队操作：

从尾部获取下一个读取票据。
等待轮次（2 * (票据 / 容量) + 1）以读取槽位（票据 % 容量）。
设置轮次 = 轮次 + 1，通知写入者读取完成。

![image-20251115174901634](.\img\mpmc.png)

